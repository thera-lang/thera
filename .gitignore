# Compiled Hawk bytecode (build artifacts; emit/run write these anywhere).
*.thera-bc
# …except the checked-in self-hosting bootstrap (see bootstrap/README.md).
!bootstrap/frontend.thera-bc

# The assembled binary SDK + build intermediates (see bin/build_sdk.sh).
build/

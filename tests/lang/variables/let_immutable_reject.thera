//! spec: var-let-immutable
//! mode: check

fn main() -> Int {
    let n = 5;
    // Reassigning a binding that isn't `mut` is an error.
    n = 6;   // expect error: cannot assign to `n`
    return 0;
}

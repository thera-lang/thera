//! spec: var-let-immutable
//! mode: check

// Parameters are immutable bindings too — reassigning one is an error.
fn inc(_ x: Int) -> Int {
    x = x + 1;   // expect error: cannot assign to `x`
    return x;
}

fn main() -> Int {
    return inc(1);
}

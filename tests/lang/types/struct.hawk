//! spec: type-struct
//! mode: run

struct Point {
    let x: Int;
    let y: Int;
}

fn main() -> Int {
    let p = Point { x: 3, y: 4 };
    println('${p.x}');   // expect: 3
    println('${p.y}');   // expect: 4
    return 0;
}

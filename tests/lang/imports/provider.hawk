// Support library: a leaf the conformance fixtures import transitively.
// No `//!` directive block, so the harness treats it as a support file.

pub fn provide() -> Int {
    return 7;
}

pub struct Gadget {
    n: Int,
}

// Not `pub`: reaching `provider.Hidden` from another file is a visibility error.
struct Hidden {
    n: Int,
}

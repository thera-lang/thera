//! spec: mod-import-ns
//! mode: run

import std.math;

// The last path segment (`math`) becomes the namespace.
fn main() -> Int {
    println('${math.sqrt(16.0)}');   // expect: 4.0
    return 0;
}

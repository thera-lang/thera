//! spec: vis-pub
//! mode: check

import 'greeter';

fn main() -> Int {
    // greeter.secret() is not `pub`; reaching it from another file is an error.
    let s = greeter.secret();   // expect error: `secret` is not a public member of `greeter`
    println('${s}');
    return 0;
}

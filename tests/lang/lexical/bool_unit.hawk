//! spec: lex-bool-unit
//! mode: run

fn main() -> Int {
    println('${true}');       // expect: true
    println('${false}');      // expect: false
    // `void` is the single value of type Void.
    let nothing = void;
    println('void bound');    // expect: void bound
    return 0;
}

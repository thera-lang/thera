//! spec: iface-display
//! mode: check

type Bare = {
    n: Int,
}

fn main() -> Int {
    let b = Bare { n: 1 };
    // `${}` requires Display; Bare has no impl, so this is a compile error.
    println('${b}');   // expect error: does not implement Display
    return 0;
}

//! spec: iface-dispatch
//! mode: run

struct Dog {
    let name: String;
}

impl Display for Dog {
    fn to_string(self) -> String {
        return 'dog:${self.name}';
    }
}

struct Cat {
    let name: String;
}

impl Display for Cat {
    fn to_string(self) -> String {
        return 'cat:${self.name}';
    }
}

// A `List<Display>` holds different concrete types; each `to_string()` call
// dispatches dynamically on the value's runtime type.
fn main() -> Int {
    let pets: List<Display> = [Dog { name: 'rex' }, Cat { name: 'mo' }];
    for p in pets {
        println(p.to_string());
    }
    // expect: dog:rex
    // expect: cat:mo
    return 0;
}
